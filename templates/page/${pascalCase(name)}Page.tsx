import React from 'react';
import styles from './${pascalCase(name)}Page.module.less';

const ${pascalCase(name)}Page = () => {
  return (
    <div className={`\${styles.root}`}>

    </div>
  );
};

export default ${pascalCase(name)}Page;
